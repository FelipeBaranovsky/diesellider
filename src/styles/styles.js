import { StyleSheet } from 'react-native';

export const globalStyles = StyleSheet.create({
  text: {
    fontSize: 16,
    color: 'black',
    // fontFamily: 'Mono Sans',
    // Otros estilos que desees aplicar a todos los componentes Text
  },
});
